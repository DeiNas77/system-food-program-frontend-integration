/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFoodDecoder,
  getFoodEncoder,
  type Food,
  type FoodArgs,
} from "../types";

export const INVENTORY_FOOD_DISCRIMINATOR = new Uint8Array([
  232, 121, 243, 152, 167, 118, 49, 117,
]);

export function getInventoryFoodDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INVENTORY_FOOD_DISCRIMINATOR,
  );
}

export type InventoryFood = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  name: string;
  foods: Array<Food>;
};

export type InventoryFoodArgs = {
  owner: Address;
  name: string;
  foods: Array<FoodArgs>;
};

/** Gets the encoder for {@link InventoryFoodArgs} account data. */
export function getInventoryFoodEncoder(): Encoder<InventoryFoodArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["foods", getArrayEncoder(getFoodEncoder())],
    ]),
    (value) => ({ ...value, discriminator: INVENTORY_FOOD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link InventoryFood} account data. */
export function getInventoryFoodDecoder(): Decoder<InventoryFood> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["foods", getArrayDecoder(getFoodDecoder())],
  ]);
}

/** Gets the codec for {@link InventoryFood} account data. */
export function getInventoryFoodCodec(): Codec<
  InventoryFoodArgs,
  InventoryFood
> {
  return combineCodec(getInventoryFoodEncoder(), getInventoryFoodDecoder());
}

export function decodeInventoryFood<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<InventoryFood, TAddress>;
export function decodeInventoryFood<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<InventoryFood, TAddress>;
export function decodeInventoryFood<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<InventoryFood, TAddress> | MaybeAccount<InventoryFood, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getInventoryFoodDecoder(),
  );
}

export async function fetchInventoryFood<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<InventoryFood, TAddress>> {
  const maybeAccount = await fetchMaybeInventoryFood(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeInventoryFood<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<InventoryFood, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeInventoryFood(maybeAccount);
}

export async function fetchAllInventoryFood(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<InventoryFood>[]> {
  const maybeAccounts = await fetchAllMaybeInventoryFood(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeInventoryFood(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<InventoryFood>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeInventoryFood(maybeAccount));
}
