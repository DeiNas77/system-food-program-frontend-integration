/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { FOOD_INVENTORY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SHOW_FOODS_DISCRIMINATOR = new Uint8Array([
  96, 214, 215, 92, 117, 90, 35, 212,
]);

export function getShowFoodsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SHOW_FOODS_DISCRIMINATOR);
}

export type ShowFoodsInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountInventoryFood extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountInventoryFood extends string
        ? WritableAccount<TAccountInventoryFood>
        : TAccountInventoryFood,
      ...TRemainingAccounts,
    ]
  >;

export type ShowFoodsInstructionData = { discriminator: ReadonlyUint8Array };

export type ShowFoodsInstructionDataArgs = {};

export function getShowFoodsInstructionDataEncoder(): FixedSizeEncoder<ShowFoodsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SHOW_FOODS_DISCRIMINATOR }),
  );
}

export function getShowFoodsInstructionDataDecoder(): FixedSizeDecoder<ShowFoodsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getShowFoodsInstructionDataCodec(): FixedSizeCodec<
  ShowFoodsInstructionDataArgs,
  ShowFoodsInstructionData
> {
  return combineCodec(
    getShowFoodsInstructionDataEncoder(),
    getShowFoodsInstructionDataDecoder(),
  );
}

export type ShowFoodsInput<
  TAccountOwner extends string = string,
  TAccountInventoryFood extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  inventoryFood: Address<TAccountInventoryFood>;
};

export function getShowFoodsInstruction<
  TAccountOwner extends string,
  TAccountInventoryFood extends string,
  TProgramAddress extends Address = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
>(
  input: ShowFoodsInput<TAccountOwner, TAccountInventoryFood>,
  config?: { programAddress?: TProgramAddress },
): ShowFoodsInstruction<TProgramAddress, TAccountOwner, TAccountInventoryFood> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FOOD_INVENTORY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    inventoryFood: { value: input.inventoryFood ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.inventoryFood),
    ],
    data: getShowFoodsInstructionDataEncoder().encode({}),
    programAddress,
  } as ShowFoodsInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood
  >);
}

export type ParsedShowFoodsInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    inventoryFood: TAccountMetas[1];
  };
  data: ShowFoodsInstructionData;
};

export function parseShowFoodsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedShowFoodsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), inventoryFood: getNextAccount() },
    data: getShowFoodsInstructionDataDecoder().decode(instruction.data),
  };
}
