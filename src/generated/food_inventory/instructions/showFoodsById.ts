/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { FOOD_INVENTORY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SHOW_FOODS_BY_ID_DISCRIMINATOR = new Uint8Array([
  7, 46, 28, 114, 145, 163, 53, 243,
]);

export function getShowFoodsByIdDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SHOW_FOODS_BY_ID_DISCRIMINATOR,
  );
}

export type ShowFoodsByIdInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountInventoryFood extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountInventoryFood extends string
        ? WritableAccount<TAccountInventoryFood>
        : TAccountInventoryFood,
      ...TRemainingAccounts,
    ]
  >;

export type ShowFoodsByIdInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type ShowFoodsByIdInstructionDataArgs = { name: string };

export function getShowFoodsByIdInstructionDataEncoder(): Encoder<ShowFoodsByIdInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SHOW_FOODS_BY_ID_DISCRIMINATOR }),
  );
}

export function getShowFoodsByIdInstructionDataDecoder(): Decoder<ShowFoodsByIdInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getShowFoodsByIdInstructionDataCodec(): Codec<
  ShowFoodsByIdInstructionDataArgs,
  ShowFoodsByIdInstructionData
> {
  return combineCodec(
    getShowFoodsByIdInstructionDataEncoder(),
    getShowFoodsByIdInstructionDataDecoder(),
  );
}

export type ShowFoodsByIdInput<
  TAccountOwner extends string = string,
  TAccountInventoryFood extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  inventoryFood: Address<TAccountInventoryFood>;
  name: ShowFoodsByIdInstructionDataArgs["name"];
};

export function getShowFoodsByIdInstruction<
  TAccountOwner extends string,
  TAccountInventoryFood extends string,
  TProgramAddress extends Address = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
>(
  input: ShowFoodsByIdInput<TAccountOwner, TAccountInventoryFood>,
  config?: { programAddress?: TProgramAddress },
): ShowFoodsByIdInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountInventoryFood
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FOOD_INVENTORY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    inventoryFood: { value: input.inventoryFood ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.inventoryFood),
    ],
    data: getShowFoodsByIdInstructionDataEncoder().encode(
      args as ShowFoodsByIdInstructionDataArgs,
    ),
    programAddress,
  } as ShowFoodsByIdInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood
  >);
}

export type ParsedShowFoodsByIdInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    inventoryFood: TAccountMetas[1];
  };
  data: ShowFoodsByIdInstructionData;
};

export function parseShowFoodsByIdInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedShowFoodsByIdInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), inventoryFood: getNextAccount() },
    data: getShowFoodsByIdInstructionDataDecoder().decode(instruction.data),
  };
}
