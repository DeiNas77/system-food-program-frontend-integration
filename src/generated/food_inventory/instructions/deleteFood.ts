/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { FOOD_INVENTORY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const DELETE_FOOD_DISCRIMINATOR = new Uint8Array([
  49, 236, 103, 149, 25, 110, 136, 25,
]);

export function getDeleteFoodDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DELETE_FOOD_DISCRIMINATOR);
}

export type DeleteFoodInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountInventoryFood extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountInventoryFood extends string
        ? WritableAccount<TAccountInventoryFood>
        : TAccountInventoryFood,
      ...TRemainingAccounts,
    ]
  >;

export type DeleteFoodInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type DeleteFoodInstructionDataArgs = { name: string };

export function getDeleteFoodInstructionDataEncoder(): Encoder<DeleteFoodInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DELETE_FOOD_DISCRIMINATOR }),
  );
}

export function getDeleteFoodInstructionDataDecoder(): Decoder<DeleteFoodInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDeleteFoodInstructionDataCodec(): Codec<
  DeleteFoodInstructionDataArgs,
  DeleteFoodInstructionData
> {
  return combineCodec(
    getDeleteFoodInstructionDataEncoder(),
    getDeleteFoodInstructionDataDecoder(),
  );
}

export type DeleteFoodInput<
  TAccountOwner extends string = string,
  TAccountInventoryFood extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  inventoryFood: Address<TAccountInventoryFood>;
  name: DeleteFoodInstructionDataArgs["name"];
};

export function getDeleteFoodInstruction<
  TAccountOwner extends string,
  TAccountInventoryFood extends string,
  TProgramAddress extends Address = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
>(
  input: DeleteFoodInput<TAccountOwner, TAccountInventoryFood>,
  config?: { programAddress?: TProgramAddress },
): DeleteFoodInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountInventoryFood
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FOOD_INVENTORY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    inventoryFood: { value: input.inventoryFood ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.inventoryFood),
    ],
    data: getDeleteFoodInstructionDataEncoder().encode(
      args as DeleteFoodInstructionDataArgs,
    ),
    programAddress,
  } as DeleteFoodInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood
  >);
}

export type ParsedDeleteFoodInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    inventoryFood: TAccountMetas[1];
  };
  data: DeleteFoodInstructionData;
};

export function parseDeleteFoodInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeleteFoodInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), inventoryFood: getNextAccount() },
    data: getDeleteFoodInstructionDataDecoder().decode(instruction.data),
  };
}
