/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findInventoryFoodPda } from "../pdas";
import { FOOD_INVENTORY_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_SYSTEM_FOOD_DISCRIMINATOR = new Uint8Array([
  98, 179, 36, 82, 24, 210, 5, 94,
]);

export function getCreateSystemFoodDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_SYSTEM_FOOD_DISCRIMINATOR,
  );
}

export type CreateSystemFoodInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountInventoryFood extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountInventoryFood extends string
        ? WritableAccount<TAccountInventoryFood>
        : TAccountInventoryFood,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateSystemFoodInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
};

export type CreateSystemFoodInstructionDataArgs = { name: string };

export function getCreateSystemFoodInstructionDataEncoder(): Encoder<CreateSystemFoodInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_SYSTEM_FOOD_DISCRIMINATOR }),
  );
}

export function getCreateSystemFoodInstructionDataDecoder(): Decoder<CreateSystemFoodInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateSystemFoodInstructionDataCodec(): Codec<
  CreateSystemFoodInstructionDataArgs,
  CreateSystemFoodInstructionData
> {
  return combineCodec(
    getCreateSystemFoodInstructionDataEncoder(),
    getCreateSystemFoodInstructionDataDecoder(),
  );
}

export type CreateSystemFoodAsyncInput<
  TAccountOwner extends string = string,
  TAccountInventoryFood extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  inventoryFood?: Address<TAccountInventoryFood>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateSystemFoodInstructionDataArgs["name"];
};

export async function getCreateSystemFoodInstructionAsync<
  TAccountOwner extends string,
  TAccountInventoryFood extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
>(
  input: CreateSystemFoodAsyncInput<
    TAccountOwner,
    TAccountInventoryFood,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateSystemFoodInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FOOD_INVENTORY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    inventoryFood: { value: input.inventoryFood ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.inventoryFood.value) {
    accounts.inventoryFood.value = await findInventoryFoodPda({
      owner: expectAddress(accounts.owner.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.inventoryFood),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateSystemFoodInstructionDataEncoder().encode(
      args as CreateSystemFoodInstructionDataArgs,
    ),
    programAddress,
  } as CreateSystemFoodInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood,
    TAccountSystemProgram
  >);
}

export type CreateSystemFoodInput<
  TAccountOwner extends string = string,
  TAccountInventoryFood extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  inventoryFood: Address<TAccountInventoryFood>;
  systemProgram?: Address<TAccountSystemProgram>;
  name: CreateSystemFoodInstructionDataArgs["name"];
};

export function getCreateSystemFoodInstruction<
  TAccountOwner extends string,
  TAccountInventoryFood extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
>(
  input: CreateSystemFoodInput<
    TAccountOwner,
    TAccountInventoryFood,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateSystemFoodInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountInventoryFood,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? FOOD_INVENTORY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    inventoryFood: { value: input.inventoryFood ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.inventoryFood),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateSystemFoodInstructionDataEncoder().encode(
      args as CreateSystemFoodInstructionDataArgs,
    ),
    programAddress,
  } as CreateSystemFoodInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountInventoryFood,
    TAccountSystemProgram
  >);
}

export type ParsedCreateSystemFoodInstruction<
  TProgram extends string = typeof FOOD_INVENTORY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    inventoryFood: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateSystemFoodInstructionData;
};

export function parseCreateSystemFoodInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateSystemFoodInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      inventoryFood: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateSystemFoodInstructionDataDecoder().decode(instruction.data),
  };
}
